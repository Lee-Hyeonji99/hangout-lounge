# hangout-lounge

앉기/채팅/먹기 같은 캐주얼 3D 소셜 라운지. 원래 `Lee-Hyeonji99/web-games`(정적
미니게임 아케이드) 안에서 기획하다가, 운영 성격이 근본적으로 달라서
(상시 시그널링 서버, 실시간 다인 상호작용, 잠재적 모더레이션 필요) 별도
저장소로 분리했다. 두 저장소는 코드로 묶여있지 않다 — 필요한 모듈은
jsDelivr의 GitHub CDN(`cdn.jsdelivr.net/gh/...`)으로 서로 직접 import한다
(패키지 매니저/빌드 없이도 가능, 이 프로젝트도 web-games도 빌드 단계가 없다는
전제는 유지).

## 원칙

- **정적 파일 + 빌드 없음**이 기본(=web-games와 동일 철학) — **단 하나의 예외는
  WebRTC 시그널링**. 자세한 이유와 선택지는 `docs/PLAN.md` 3-a 참고. 이 예외
  말고는 새 서버/빌드 단계를 추가하지 않는다.
- **동접 목표 12명** — P2P 메시 방식의 실용 한계에 걸치는 숫자다. 이 이상으로
  늘려야 하면 아키텍처를 다시 검토해야 한다(`docs/PLAN.md` 3번).
- **이모지를 UI에 직접 쓰지 않는다.** 아이콘은 인라인 SVG로만(OS마다 이모지
  모양이 달라 톤이 깨진다) — web-games와 같은 규칙.

## 스택

- Three.js(ES 모듈) — `lib/vendor/three/build/three.module.js`로 **로컬 벤더링**해서 쓴다.
  web-games가 CDN import만 쓰다가 CDN이 막힌 네트워크에서 게임이 통째로 안 뜨는
  버그를 실제로 겪은 적이 있어서(그 수정 이력이 web-games repo에 남아있음),
  이 저장소는 처음부터 벤더링으로 시작한다. 버전 올릴 때만 수동으로 파일 교체.
- 아바타: 원래 계획은 **Quaternius**(CC0, glTF, Universal Animation Library —
  `docs/PLAN.md` 5번)였는데, 이 작업 환경의 네트워크 프록시가 Quaternius/Kenney
  같은 에셋 사이트 도메인을 막고 있어서(조직 정책 allowlist, GitHub만 허용)
  받을 수가 없었다. 임시로 three.js 공식 예제 에셋(soldier.glb, 군인 모델)을
  썼다가 톤이 안 맞고 외부 파일 의존을 없애자는 방향으로 바뀌어서, 지금은
  **`index.html`의 `buildHamster()`가 Three.js 기본 도형(구)만 조합해서 직접
  그리는 귀여운 햄스터 아바타**로 교체했다 — glTF 파일도, 외부 저장소 의존도
  없음. 애니메이션은 클립이 없으니 `spawnAvatar()`의 `mixer.update(dt)`가
  state(Idle/Walk)에 따라 통통 튀는 움직임·귀 흔들림·발 움직임을 매 프레임
  직접 계산해서 만든다(`THREE.AnimationMixer`가 아니라 같은 인터페이스만
  흉내낸 객체). **더 디테일한 아바타가 필요해지면(Quaternius 등) 이 함수만
  glTF 로더로 교체**하면 된다.
- 네트워킹: WebRTC(PeerJS로 시그널링 시작) — `docs/PLAN.md` 3번 전체가 설계.
- 애니메이션 재생: 상태 이름 문자열(Idle/Walk)만 네트워크로 전송, 실제 움직임
  계산은 각자 클라이언트가 로컬에서(`docs/PLAN.md` 4번). 현재 아바타는 glTF
  클립이 없어서 `THREE.AnimationMixer` 대신 직접 짠 파츠 애니메이션을 쓴다
  (위 "아바타" 항목).

## 아트 스타일

아직 이 프로젝트만의 팔레트/톤이 확정되지 않았다 — web-games의 정확한 HEX값을
그대로 베끼지 않는다(그건 그 아케이드 허브용 톤이고, 이 프로젝트는 별개
정체성이다). 다만 **방법론은 그대로 가져온다**(왜 효과적인지는 web-games의
`docs/ART_STYLE.md` "5-b. 3D 게임 규칙" 참고):

1. 팔레트를 10~13색으로 고정하고 코드에서 즉석으로 색을 만들지 않는다.
2. 모든 표면이 같은 미세 결(grain) 범프를 공유한다.
3. `emissive`는 실제 광원에만 쓴다.
4. 하드서피스는 같은 모서리 반경을 쓴다.
5. **에셋 대조 확인 습관** — 새 3D 소품을 추가/수정하면 실제 씬 오브젝트를
   줄세워 눈으로 비교하는 모드를 만들어 두고 쓴다(web-games의 `?contact=1`이
   한 번 방치돼서 깨진 전례가 있다 — 만들어두는 것과 "실제로 계속 쓰는 것"은
   다른 문제라는 교훈만 가져온다).

## 문서

- `docs/PLAN.md` — 전체 기술 설계(스코프, 아키텍처, 상태 동기화, 아바타, 룸
  모델, 구현 순서, 착수 전 결정 사항 체크리스트). **구현 시작 전 반드시 먼저
  읽는다.**

## 진행 상태

3단계(WebRTC 메시 연결)까지 코드는 있음 — `index.html` + `lib/webrtc-mesh.js`.
**단, 실제 P2P 연결 자체는 이 개발 환경에서 검증 못 함** — 이 환경의 네트워크
프록시가 PeerJS 기본 브로커(`0.peerjs.com`)도 막고 있어서(Quaternius/jsDelivr/
unpkg와 같은 조직 정책 allowlist 문제) `Peer` 객체가 브로커에 연결을 못 한다.
코드 자체(아바타 로드·WASD 이동·카메라 추적·UI·보간 수식)는 Playwright로
확인했고 PeerJS 쪽은 "정상적으로 연결을 시도하다 네트워크 에러로 실패"까지만
관찰함(코드 버그가 아니라 이 환경의 제약). **실제 브라우저 두 탭으로
`index.html` 열어서 한쪽 ID를 복사해 다른 쪽 "상대 ID" 칸에 붙여넣고 연결
버튼을 눌러 직접 검증 필요** — 또는 `index.html?connect=<상대ID>`로 열면
자동 연결 시도.

다음 단계는 `docs/PLAN.md` 8번(구현 순서) 4번(음성 추가)부터, 단 위 3단계
실제 연결 검증이 먼저 끝나야 한다.
